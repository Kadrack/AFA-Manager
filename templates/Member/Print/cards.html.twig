<!DOCTYPE html>
<html lang="fr" style="margin: 0">
<head>
    <meta content="text/html; charset=UTF-8" http-equiv="Content-Type">
    <style>
        .conteneurCarte
        {
            position:relative;
            width:8.5cm;
            height:5.5cm;
            color:white;
            padding:0;
        }

        img.photo
        {
            position:absolute;
            width:1.87cm;
            top:0.65cm;
            left:6.52cm;
            max-height: 110px;
        }

        img.licence
        {
            position:absolute;
            width:8.6cm;
            height:5.4cm;
            top:0;
        }

        img.codebarre
        {
            position:absolute;
            width:2.8cm;
            height:1cm;
            top:4.15cm;
            left:5.20cm;
        }

        .nomPrenom
        {
            position:absolute;
            top:2.9cm;
            width:8.3cm;
            text-align:right;
            color:white;
            font-family: verdana, arial, sans-serif;
            font-size:0.8em;
        }

        .club
        {
            position:absolute;
            top:3.25cm;
            width:8.3cm;
            text-align:right;
            color:white;
            font-family: verdana, arial, sans-serif;
            font-size:0.7em;
        }

        .licence
        {
            position:absolute;
            top:3.7cm;
            width:8.2cm;
            text-align:right;
            color:red;
            font-family: verdana, arial, sans-serif;
            font-size:0.8em;
        }
    </style>
    <title>Carte membre</title>
</head>
<body style="margin: 0">
    <div class="conteneurCarte">
        <div>&nbsp;</div>
        <img class="licence" src="{{ asset('images/licence.jpg') }}" alt="N° licence"/>
        <img class="photo" src="{{ data.Member.getMemberPhoto }}" alt="Photo"/>
        <img class="codebarre" src="data:image/png;base64,{{ data.Barcode }}" alt="Code barre"/>
        <div class="nomPrenom">{{ data.Member.memberFirstname|title }} {{ data.Member.memberName|title }}</div>
        <div class="club">{{ data.Member.getMemberLicences.0.getMemberLicenceClub.clubName|title }} - {{ data.Member.getMemberLicences.0.getMemberLicenceClub.clubId }}</div>
        <div class="licence"><b>Licence n° {{ data.Member.memberId }}</b></div>
    </div>
</body>
</html>
