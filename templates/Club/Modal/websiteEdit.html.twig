<div class="modal-dialog modal-dialog-centered" role="document">
    <div class="modal-content">
        <div class="modal-header">
            <h5 class="modal-title">Modifier détails site internet</h5>
        </div>
        <div class="modal-body">
            {{ form_start(form) }}
            {{ form_row(form.ClubUrl) }}
            {{ form_row(form.ClubContactPublic) }}
            {{ form_row(form.ClubPhonePublic) }}
            {{ form_row(form.ClubEmailPublic) }}
            <div class="d-grid gap-0">{{ form_widget(form.Submit) }}</div>
            {{ form_end(form) }}
        </div>
        <div class="modal-footer">
            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Annuler</button>
        </div>
    </div>
</div>
